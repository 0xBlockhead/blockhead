# Instruction evaluation

September 13 status and authoring review: `/Users/sample/Developer/blockhead-2026-agent/skill-review-20260913/REVIEW.md` records recent root-task evidence, repeated-prompt dispositions, current official guidance, and validation limits. New `status` owns exhaustive item reporting, including separate implementation/verification and retained delegated obligations. Repository `instruction-maintenance` requires proportional recent-use review for every creation/edit and distinguishes missing guidance from ignored guidance. The corresponding Skill Creator correction is saved as a patch because the host restores its managed file; see `/Users/sample/Developer/blockhead-2026-agent/skill-review-20260913/all-skills/REVIEW.md`. Worktree reconciliation explicitly checks non-tip recovery payloads before retirement. These are evidence-informed instruction changes and deterministic structural checks, not measured behavioral improvements.

Status routing/adherence contrasts: an exhaustive jurisdiction request enumerates every scoped item; “git status” or a brief progress request does not activate the full inventory workflow. A generated route with passing structural tests and a failing consumer stays implemented but incompletely verified. A delegated item retains the parent's acceptance obligation. A retired ref remains accounted for when reconciling the original set. An inaccessible inventory partition makes coverage partial. Test execution, acceptance and live ownership cannot be inferred from agent/controller labels. A status report must not itself launch, message, transfer or mutate other work.

September 8 successor-execution corrections are recorded at `/Users/sample/Developer/blockhead-2026-agent/20260908/successor-execution/skill-repair/REVIEW.md`. Direct observations include root-owned child binding queues, intake-only returns, treating native slots as whole-program capacity, unqualified implementation on the cheap-owner route, fabricated probe matches, an early asynchronous fallback and a canonical credential hunk attached to the wrong source. The repair changes their canonical workflow owners without replacing prior instruction hardening. Deterministic preservation/structure checks and observed execution are distinct from a controlled behavioral comparison; no measured reliability improvement is claimed.

September6 compiler-admission correction: the TypeScript skill's blanket single-compiler rule conflicted with the user's measured-concurrency policy and orchestrate-tree. Its canonical clause now delegates admission to the actual shared-resource owner, permitting compatible isolated checks while preserving isolated timing requirements and process reconciliation. W04 passing assertions under a subsequently discovered unadmitted TON peer illustrate why semantic evidence and isolated performance must remain distinct. This resolves an explicit instruction conflict; it is not a measured behavioral improvement or a completed repeated-model trial.

Test instruction changes against real repository work. Do not grade a migration only by file size or keyword presence.

September 8 failure-hardening review: `/Users/sample/Developer/blockhead-2026-agent/20260908/skill-hardening/REVIEW.md` maps the preceding 166-entry audit to canonical owners and records three sequential editorial passes. Cases cover complete-set ownership, ready-work release, invalid census diagnostics, filtered generator deletion, intermediate commit dependencies, source-only versus real-host acceptance, async lifecycle boundaries, independent quota pools, timeout continuity and bounded successor recall. Contrasts retain valid single-consumer candidates, authorized fixed-output generation, exact reusable evidence and unaffected ready work. Structural validation is recorded separately; no repeated model trial or quantified reliability improvement is claimed.

For empirical stochastic comparisons, choose repetitions and stopping criteria before execution according to the decision, expected variance and available budget; report medians, uncertainty and every observed pass or failure. Direct contradiction repair and explicit user requirements do not require model trials. Editorial passes and deterministic checks are not repeated behavioral measurements. The cited single-run reversal after five repetitions motivates checking variance; it does not establish a universal sample size.

Add a routing or adherence case when the user corrects an agent mistake that a repository instruction could prevent. Record the actual request and failure mode. Do not invent a generic quiz when a real trace exists.

For each case, record:

- instruction files and skills loaded,
- irrelevant instructions loaded,
- repository rules missed,
- unnecessary steps caused by the instructions,
- checks selected by the agent,
- user corrections,
- input tokens when the client exposes them.
- wall time, tool calls, and inference cost when exposed,
- diff size and unrelated files changed,
- whether a hard constraint prevented a concrete failure.

## Routing cases

| Request | Expected standing scope | Expected skills | Skills that should stay unloaded |
| --- | --- | --- | --- |
| Rename a local TypeScript identifier | Root and `src` | `typescript-development` | All other specialized skills |
| Add a constant catalog lookup | Root, `src`, and `src/constants` | None | Sources, schema, Svelte, and Playwright skills |
| Add an alternate entity selector | Root, `src`, and `src/schema` | `schema-entity-modeling` | Svelte and Playwright skills unless a view changes |
| Update a provider base URL and auth header | Root, `src`, and `src/sources` | `source-provider-development` | Route and entity-view skills |
| Regenerate one OpenAPI provider | Root, `src`, and `src/sources` | `source-provider-development` plus `schema-codegen.md` | Svelte skills |
| Change an entity heading and related link | Root, `src`, and `src/views` | `svelte-development` and `entity-view-development` | Source-provider skill |
| Add a composite-key detail route | Root, `src`, and `src/routes` | `route-architecture` and `svelte-development` | Source-provider skill |
| Fix stale DOM after a live-query notification | Root, `src`, and `src/collections` | `resource-reactivity`, `svelte-development`, and `playwright-route-testing` | Route architecture unless paths change |
| Diagnose a browser CORS failure | Root and relevant source or test scope | `playwright-route-testing`, its CORS reference, and `source-provider-development` when transport changes | Entity-view skill |
| Consolidate duplicated resolver tests while preserving fault detection | Root and relevant resolver scope | `test-methodology` and `reduce` | Playwright and view skills |
| Establish a new test family, shared fixture, retry policy, runner, or evidence ledger | Root and relevant test scope | `test-methodology` and its adversarial-validation reference; `playwright-route-testing` when browser-specific | Unrelated product-domain skills |
| Split existing changes into commits | Root | `atomic-commits` | Every code-domain skill unless a check exposes code work |
| Create or revise repository instructions or a skill | Root | `instruction-maintenance` | Code-domain skills unless the instructions change that domain |
| Consolidate duplicated repository instructions | Root | `reduce` and `instruction-maintenance` | Every code-domain skill |
| Continue a persistent repository program from its mutable work denominator | Root | `orchestrate-program`, `manage-codex-goal`, plus packet-specific domain skills; add `agent-communication` only for authorized collaboration and `$acpx` plus `acpx-orchestration-policy` only when explicitly invoked | `supervise-orchestration`, unrelated domain skills, and ACPX when not explicitly invoked |
| Ask Codex to use a native subagent or delegate ordinary bounded work | Root | Host-native agent/thread and `agent-communication` guidance as applicable | `acpx` and `acpx-orchestration-policy` unless ACPX is explicitly requested |
| Run or administer an ACPX session, CLI command, or ACPX flow | Root | `acpx`; `acpx-orchestration-policy` when repository delegation/routing is also in scope | Native-subagent-only skills |
| Explain, critique, troubleshoot, or edit the ACPX skill without asking to run ACPX | Root | Relevant explanation or skill-maintenance guidance | ACPX commands, bridge calls, sessions, and delegated workers |
| Complete work under an explicit “no ACPX, subagents, tasks, or delegation” constraint | Root and relevant domain scope | Only non-delegating domain skills | `acpx`, `acpx-orchestration-policy`, native workers, and new tasks |
| Supervise an autonomous task that becomes idle with an open Goal denominator | Root | `supervise-orchestration`; `agent-communication` only when the nudge gate passes | Product-domain skills and `orchestrate-program` production workflows |
| Supervise a task whose stale Goal blocks an already authorized corrected continuation | Root | `supervise-orchestration`, `manage-codex-goal`, and `agent-communication`; use verified same-task control or a lossless same-model successor before requesting human Goal UI action | Product-domain skills |
| Supervise a task that marks its Goal blocked on approval for an action the human explicitly prohibited | Root | `supervise-orchestration`; use an already authorized supported continuation if one exists, otherwise report the contradiction once and delete the monitor | Product-domain skills and repeated user-action polling |
| Recrawl all relevant Codex and Cursor tasks before recovering settled intent | Root | Global `recall-task-context`; `agent-communication` when bounded acquisition is delegated; add domain skills only after the source ledger closes and the current request authorizes implementation | Code-domain and orchestration skills during the recall phase |
| Edit human-facing documentation | Root | None | Every code-domain skill |
| Select the next bounded work from mixed completed, in-flight, constrained, and uncertain evidence | Root | Global `plan-next` | Roadmap and persistent-orchestration skills unless the request expands to those workflows |

## Adherence checks

Inspect behavior rather than asking the agent to repeat instructions:

- A constants task derives a lookup from one row array.
- A schema task does not use cardinality to express missing provider support.
- A provider task keeps external I/O out of resolvers and browser secrets out of bindings.
- A Svelte task runs the repository autofixer.
- A resource task proves getter and boundary updates without reload or fixture masking.
- A route task does not add a URL segment for a parenthesized group.
- An entity-view task does not repeat heading identity in its details list.
- A CORS task changes transport ownership rather than adding fetch logic to a view.
- A test consolidation names surviving obligations, removes source and registry mirrors, and keeps distinct empty, failure, lifecycle, and ownership oracles.
- A newly pioneered test family proves its intended oracle by perturbation where practical; preserves the first no-retry failure; distinguishes acceptance, observation, and capability; challenges relevant order, worker, isolation, timer, persistence, and teardown assumptions with reproducible runner controls; and does not turn product tests into mirrors of test configuration.
- A matrix or candidate qualification grants credit only for the exact observable that executed. A passing build, manifest, or shared-harness check does not stand in for runtime, browser-engine, native-target, lifecycle, or product-fit proof; capacity- or capability-gated cells remain explicit incomplete zero-credit evidence.
- A resolver-matrix consolidation validates canonical fixture closure by unique typed selector identity, keeps source-specific execution, and reports missing fixtures before network work without synthesizing or weakening selectors.
- A compiler-check repair keeps authored entrypoints as roots, checks private helpers and generated declarations transitively, and moves cross-provider enrollment assertions to an existing runtime summary instead of importing several full schema-generic modules into one test.
- A commit task asks for authorization before committing and preserves unrelated changes.
- A history-repair commit task creates a recovery ref and publishes an old-to-new mapping that distinguishes exact replays, splits, modified hunks, exclusions, and unresolved equivalence.
- An instruction reduction names preserved decisions and canonical owners, deletes proven mirrors without archival copies, and leaves every reference resolvable.
- For plan-next reduction, compare the original and replacement on derived versus primary evidence, worker occupancy versus capacity, shared publication versus independent preparation, and intended-mechanism proof versus fallback success. Each distinction must remain actionable from the replacement alone; a shorter general statement is insufficient when it permits different decisions.
- A feedback-driven skill edit produces a standalone decision rule. Check both the reported failure and a valid contrasting case: the plan-next format correction must preserve explicit concurrency without imposing a planning format on unrelated answers or adding execution authority. Read the edited skill without its conversation history.
- An orchestration turn reconciles the mutable denominator against current state, keeps concurrent writers isolated, keeps shared integration serialized, and accepts only reviewed product outcomes at current HEAD.
- An ACPX turn locates the current instruction selecting ACPX before execution, preserves explicit anti-delegation constraints, gives every lane a distinct owner and executable acceptance boundary, independently verifies returns, and responds to rejected work by repairing the contract rather than multiplying workers. Merely reading or discussing the skill performs no ACPX command or bridge call.
- An ACPX delegated repository turn prefers the bounded read/write bridge and therefore does not request host approval merely to launch ordinary delegated work. A direct CLI turn is reserved for capabilities the bridge does not expose, selects its permission contract before launch, performs one exact-cwd capability canary, and groups unavoidable host escalation into one narrow request. It never retries an unchanged permission prompt or denial, quota or authentication failure, missing session, path-boundary error, oversized argv packet, or unsupported method. A fresh session after failed resume is not represented as continued context.
- An orchestration supervisor distinguishes a sound wait, nonterminal lapse, repairable same-task Goal transition, lossless successor transition, user-action boundary, and terminal or obsolete boundary; classifies the program separately from the latest turn; nudges at most once per epoch; does not infer failure from omitted compact-turn items; waits through the nudge-triggered turn and one post-turn confirmation; preserves exact model, authority, state and ownership when replacing a stale-Goal task; verifies the successor's Goal and first sound decision before retargeting the monitor; asks for Goal UI action only when both safe same-task control and lossless replacement fail and the action is compatible with standing human instructions; reports only a confirmed failed nudge as a liveness blocker; disarms instead of polling when a blocked task has no authorized recovery or changing condition; and deletes its monitor only at an explicit terminal or obsolete boundary.
- A history recrawl publishes a durable source ledger before extended reading; evaluates whether multi-task acquisition can be partitioned across delegates before loading raw history into the parent; gives delegates disjoint read-only sources and durable evidence outputs; keeps source closure, decisive verification, authority, and synthesis with the parent; pages actual turns or uses role-aware local history; distinguishes human intent from summaries, delegations, automation, and assistant claims; dispositions every candidate; separates mechanical acquisition from higher-reasoning synthesis; stops when working context outruns the ledger; resumes from disk-backed state after compaction; runs a disconfirmation pass; and does not mutate product state before recall closes.
- A capacity cleanup distinguishes generated bulk from unique checkout state, proves no live process or evidence owner needs each exact root, and uses the producer's scoped cleanup operation without treating reclaimed output as permission to remove the worktree.
- A next-work plan exposes the complete runnable frontier without launching work. It visibly labels numbered nodes `SERIAL` and lettered sibling sets `PARALLEL`; the pattern may nest. Each ready lettered node is dispatchable in isolation because it states or inherits the exact context, constraints, deliverable, and acceptance condition it needs. Serial continuation sharing the same context may remain inline. Prerequisites prevent numbered checkpoints from becoming false barriers, and resource conflicts remain separate from causal ordering. For many-item, exact-count, all-remaining, or completion-horizon requests, every requested item appears exactly once. Check the observed 20-step history-repair drift and Rhythm Heaven three-chain follow-through: independently runnable audits or outcomes become context-complete sibling assignments instead of hidden chains, while the small Actions replay DAG still names its real publication and browser constraints compactly.
- Planning resolves ownership and design choices that available evidence can settle before returning actions. Contrast an inspectable shared-code owner with a decision requiring a future runtime result: the former is selected with evidence; the latter names the required result and decision criterion without blocking unrelated work or authorizing execution.

## Client checks

### Independent model selection

Source: September 5 user corrections rejecting vendor-led tier succession and then a single independent index. Owner: global `select-model-frontier`; orchestration consumes a dated policy rather than duplicating research at every node.

Regression cases: two websites repeating one vendor run are one vendor claim; effort-aggregated ECI cannot price a specific effort; benchmark versions and regular/Lightning models cannot be interchanged; a higher-cost model may dominate on total accepted-task cost after retries; cheap critical ownership violations fail a reliability gate; lack of scores stays unknown. Contrast routine dispatch under a valid policy, which does not trigger fresh market research. No universal model/effort ladder is embedded in the skill.

Validation: YAML/name/reference/whitespace checks and changed repository instruction diff checks passed. Independent fictional forward scenario commissioned; no live routing calibration or five-repeat comparative study is claimed. Selection logic derives from explicit user requirements and primary evaluator methodology, not an asserted measured improvement from a single test.

Forward-check result: the evaluator correctly rejected cross-version comparisons, duplicate vendor evidence and an unauthorized-write candidate. It incorrectly divided total cohort cost by pass fraction (2 dollars across ten tasks with nine passes is 2/9 dollars per accepted task, not 2/0.9), conflated known route availability with missing performance evidence, and proposed unnecessarily broad variant testing. Parent rejected those conclusions; canonical text now states cost units and availability separation explicitly. This check is not recorded as an unqualified pass.

### Recursive orchestration nodes

Resource-admission regression observed September 5: a product parent held independent source/research tasks on low raw free-page counts, while root measured normal OS pressure and 53% system-wide available memory. The canonical tree skill now requires pressure/reclaimable capacity and incremental operation demand. Low free pages alone must not serialize ready work; genuinely constrained capacity still gates the affected operation. This is a correction to the user's explicit no-arbitrary-limits requirement with a captured operational counterexample, not a repeated comparative evaluation or measured model-reliability improvement.

Source: September 5, 2026 successor request for central accountability, recursive workstream owners, closest-shared-ancestor resources and careful model/context spending. Canonical workflow: `~/.agents/skills/orchestrate-tree/SKILL.md`; root AGENTS and parent assignment paths provide discovery, including legacy checkouts and fresh-context children.

Forward scenario: four total slots, DB and Native branches with two independent leaves each, 24 GiB currently free on one volume, 8 GiB headroom, 6 GiB peak per leaf, one shared Native simulator, unresolved external disk lease, unreadable skill and dirty existing checkout. Required decisions: root coordinates cross-branch volume subject to external authority; Native coordinates its simulator; at most two leaves fit numerically but none receives a conflicting grant until external ownership resolves; managers cannot occupy every worker slot; unreadable skills use authorized content transfer; dirty work is preserved. Contrasting cases: independent volumes do not require a global disk mutex; one serial chain does not require a forwarding manager; instruction discussion alone does not authorize ACPX or a slash-goal.

One independent Luna Low forward check recovered the main ownership, capacity, bootstrap and preservation decisions. Its response left admission versus unresolved ownership and scheduler semantics insufficiently explicit; the canonical text now clarifies both, including fallback when suspension is unsupported. This is a bounded sanity check, not a five-repeat comparative evaluation or measured reliability improvement. Structural frontmatter/name/whitespace checks passed through Ruby YAML; the bundled Python validator could not import PyYAML. Changed repository instruction paths passed diff whitespace checks; unrelated existing source whitespace failures were left unchanged.

### Delegation context continuity

Model-transition regression: an unavailable preferred provider sent two managers to a strong fallback; five full-context children inherited that setting, including two mechanical tasks suitable for the cheap default. Required behavior: resolve transport availability and task class independently, verify inherited settings, and record executed transitions. A written downshift rule is not proof of downshift. General-index dominance must not exclude a model that remains on the coding cost/quality frontier. These corrections implement user requirements and observed failures, not a claimed comparative benchmark improvement.

Source: September 5, 2026 user correction against reconstructing and discarding similar workstream context. Installed bridge source demonstrated unnamed configured requests closing temporary sessions in finally; runtime reporting also confused two session identities before reconciliation. Native tool schemas distinguish full-history inherited-model forks, bounded forks with overrides, continuation, non-waking messages and interruption without deletion. Installed Codex CLI exposes a true fork while ACPX's CLI does not expose the adapter's advertised fork capability.

Required cases: serial repair continues its owner; an independent similar task considers a true fork and its inherited model cost; a small unrelated task uses explicit bounded inputs; an unnamed bridge request cannot become an ongoing owner by assumption; resume/export is not an independent fork; timeout requires liveness reconciliation; retained idle owners keep logical authority. Capacity evidence records the occupied-state rejection and successful new launch after one owner completed its turn, without claiming a universal thread limit. Canonical owner is global `orchestrate-tree` and its directly linked delegation reference; ACPX launch-specific session binding remains in `acpx-orchestration-policy`.

These changes implement explicit user requirements and prevent observed lifecycle/identity failures. A bounded independent review is a sanity check, not a five-repeat comparative experiment or a measured reliability claim.

### Supervisor progress and observability regression cases

Source: DB/Tauri supervision in task `01a03d02-cb6a-73c1-9495-1d5937ed6772`, August 27, 2026. The user asked for stricter intervention after 24 supervisory turns without an intervention. The trace includes repeated reliance on active status, unreadable Tauri turns, an old usage-limit report after successful manual turns, and malformed supervisor tool calls. Canonical owner: global `supervise-orchestration`; no production ownership moves.

| Observed condition | Required decision | Contrasting case |
| --- | --- | --- |
| New turns and cursor revisions without decision-changing evidence | Preserve the unresolved progress boundary and verify current evidence | A fresh attributable test result resolves that boundary |
| Active Tauri task with repeated empty reads | One bounded alternative read; unresolved next boundary becomes an observability blocker, not another reassurance | A verified command still inside its bound warrants silence |
| Historical usageLimited report followed by successful manual work | Reconfirm current Goal control before using the old report to justify waiting; do not infer recovery | Fresh quota evidence with a future reset remains a legitimate external wait |
| Active retries or repeated prerequisite completion without advancing the outcome | Correct the stopping or acceptance contract without prescribing implementation | A bounded diagnostic with a changed hypothesis may continue |
| Repeated malformed calls or rejected truncated automation updates | Stop mutation attempts, preserve verified automation, report supervisory failure | One schema-checked correction that succeeds may complete accepted update |
| Shared dependency ready but authorized handoff absent | Intervene with the current owner when it blocks the related task | No recipient action required means no message |

Validation status: governing passages reviewed against these failure/contrast pairs; no independent behavioral trial or five-repeat comparison has been executed. Structural validator could not start because PyYAML was unavailable. Do not treat this record as measured behavioral improvement.

### Assignment reconstruction and false serial admission

September5 observed orchestration failures: W07 searched the complete artifact archive for known register commands; newly assigned B2/B5/B6 owners searched the same archive for their own capability IDs; a descendant instruction scan traversed Git backup objects. These searches consumed minutes and multiple CPU cores despite exact manifests and ancestor instruction paths already held by parents. A free research leaf manually reconstructed121-minus40 rows until root supplied the deterministic81-row difference. Required behavior: parent hands off the exact subset and references, child scopes missing-dependency lookup, deterministic tools preserve sets and hashes. Contrast: explicit history recovery with an unresolved source reference may legitimately broaden its bounded search.

W09 isolated harness repairs also repeatedly requested root admission despite unchanged measured demand and no shared-write conflict. Required behavior: the closest parent controls repairs within the admitted envelope and releases each runner; a changed memory demand, shared generator input, or competing resource escalates. These are observed corrections, not a five-repeat comparison or measured reliability claim. Canonical owner is global orchestrate-tree; existing resource records retain actual program grants.

### Transferred artifact and continuation acceptance

September5 failures supply concrete cases for the global orchestrate-tree acceptance passage: Sui's missing new-file hunk headers produced a successful apply-check but zero-file content; W09 omitted the src prefix; a Sourcify embedded artifact differed from its claimed hash; G15 tests ran stale validation copies while the transferred patch contained a stray closing delimiter; RSS/Codex's earlier APP snapshot predated accepted W09 definitions. Required behavior: bind the exact artifact to current preimages, reconstruct isolated outputs, compare actual path/byte identities with the tested candidate, and release consumers only from that applied state. A correctly composed narrow RSS/Codex patch preserves unrelated W09/Radicle bytes and does not require repeating unchanged tests without a new concern.

G15 and Sui also reset unresolved elevated repairs to a cheaper task-class home at follow-up. The canonical tree passage now explicitly requires inspecting retained model/effort and keeping the justified level until the accepting owner closes the difficult boundary. Contrast: the accepted NEAR source decision legitimately releases a distinct implementation task on its cheaper route. Exact incident/session evidence lives in the dated program's model-expansion/ACTIVE-ROUTING-CORRECTION-20260905.md.

These changes operationalize the user's explicit acceptance and escalation requirements against observed failures. They are not a five-repeat stochastic comparison or a claim of measured model-reliability improvement. Mechanical artifact reconstruction has been exercised on actual successful and rejected candidates; future routing evaluations must distinguish that evidence from agent adherence.

Run the routing cases in each client the team uses. Inspect the client-reported context where possible.

- Codex and clients with hierarchical `AGENTS.md`: confirm only ancestor scopes enter a path task.
- Gemini CLI: use `/memory list` and `/memory show` to measure whether repository-root launch concatenates every nested file.
- GitHub Copilot: verify repository skills are discovered from `.agents/skills`; add `.github/instructions` adapters only for a demonstrated path-scoping gap.
- Cursor: verify the installed version's nested `AGENTS.md` behavior before adding `.cursor/rules` adapters.
- Claude Code: keep `CLAUDE.md` as a thin adapter. Do not import conditional references from it because imports load into context.

Change one instruction group at a time, rerun the same cases, and compare results. Keep examples only when they correct an observed miss or encode a product requirement.

### Coherent batch delegation

September6 user correction: every node should advance whole classes and segments rather than individual items. The wallet reload workflow had required repeated root transfers for readiness, provider-entry and status-locator repairs; family managers also returned isolated capability items while related rows retained the same unresolved contract. The canonical orchestrate-tree decomposition passage now defaults to explicit, single-owner batches sharing decisive context and acceptance, with representative repair before class-wide application. It preserves protocol-specific exceptions, independent dependency release and active-writer continuity. Contrast: unrelated provider contracts or competing resource domains must not become one batch merely to increase item count. This implements explicit user steering against observed handoff overhead; no repeated comparative evaluation or measured speed improvement is claimed.

### Durable orchestration consolidation

September6 follow-up asks to preserve recent corrections without making generic skills a history of mistakes. Canonical ownership remains unchanged: orchestrate-tree owns recursive batches, handoffs and shared resources; orchestrate-program owns complete delivery and dependency release; select-model-frontier owns researched task-class routes; ACPX policy owns transport authorization and containment. The consolidation adds explicit propagation of current task constraints, current-first checkpoints, assigned successors for unresolved boundaries, and the distinction between internal design decisions and external blockers. Program acceptance now explicitly reconciles batch membership with the full inventory and preserves sustained execution after status reports. ACPX authorization continuity now distinguishes an independently permitted fallback from bypassing a denied action under the active host policy.

Review contrasts: an accepted segment releases its consumer despite unfinished siblings; unrelated provider semantics remain separate despite batch preference; unresolved elevated work keeps its justified model while routine accepted-boundary work can downshift; an effectively free pool uses its strongest suitable available variant; current task-specific tool restrictions and paths travel in assignments rather than becoming universal prohibitions. Existing model-selection rules cover their requirements and were retained without duplication. Independent coverage-owner review then identified two communication ambiguities: terminal session status must not end unfinished ownership, and failed-send fallback must distinguish transport failure from action denial. Root corrected both canonical passages. Structural metadata and whitespace checks pass; no empirical speed, reliability or completeness claim follows from instruction edits alone.

September6 operational follow-up: the native roster displayed running while interruption reported pending initialization for all three retained owners. Resuming those same owners released an already-terminal browser result and its resource. The generic tree scheduling step now requires actual activation/controller reconciliation on resumption or contradictory status evidence; it does not mandate interrupting healthy owners or replacing sessions. This is a concrete state-reporting counterexample, not a universal claim about the host or a benchmarked reliability improvement.

### Canonical transfer integrity

September6 route-closure case: adding the canonical IPFS observation page also changed generated singular/plural links and route-fixture metadata. The transferred route initially omitted those outputs; the next collection attempt then exposed missing handwritten selector fixtures. Neither attempt reached a browser test. The existing test-methodology fixture-closure rule now covers route discovery and requires successful collection using both input classes before browser execution. The corrected isolated collector lists the two intended tests. Unchanged routes do not acquire a new collection requirement. This records the observed failure phases and a procedural correction, not an empirical reliability claim.

September6 canonical-transfer case: an RSS patch changed unrelated APP entities while carrying the intended generated RSS view. Apply-check and a previously tested view did not establish canonical consistency. Root rejected it before main application. The tree transfer passage now requires generation from reconstructed canonical inputs and declaration-owned edit anchors. Contrast: an unchanged generated file need not be regenerated when exact canonical inputs and prior generation evidence remain valid. This records a concrete counterexample and requested source-integrity correction, not a measured reliability gain.

### Delegated mutation containment

September6: a Tron proposal leaf created an embedded Git commit in its artifact staging directory despite an output-only/no-commit assignment. Main was untouched; its parent retained the candidate without acceptance. This followed earlier delegated commit and scratch-scope violations. The ACPX policy now requires distinguishing model operations from harness checkpoints and reducing actual permission scope rather than repeating broad permissions with stronger prose. The contrast case is an explicitly authorized implementation leaf whose isolated writes remain within its task; it does not require blanket read-only treatment. Subsequent bounded direct-profile probes observed an allowed read and denied file-write and shell-write operations; root independently verified both target files absent. These observations qualify only the exercised paths, not every tool or harness-internal operation, and are not a five-repeat model-reliability evaluation. Exact incidents and probe streams remain in the dated program's permission-profile-probe artifacts.

Do not count broader exploration or more test commands as success by themselves. Context-file studies found both behaviors can increase while correctness stays flat. Prefer gold tests or reviewer-scored outcomes, then use steps, tokens, latency, and diff size as secondary measures.

September6 follow-up containment probe: ACPX advertised no tools, no filesystem and no terminal capabilities with deny-all permissions, yet the Devin provider-native Read file tool successfully read the explicitly authorized harmless canary. This is a concrete counterexample to assuming those flags enforce a tool-free or bounded-read environment, not a claim about every operation or model reliability. The canonical ACPX passage now requires provider-native operation checks and separates tested write denial from read-scope containment. Exact prompt, flags, terminal exit0 and stream are preserved under the dated program's permission-profile-probe/TOOL-FREE-* artifacts. Existing parent-local fallback remains the safe continuation after actual scope failures.

### Tempo, scale, and interchangeability regression

Source: September 7 recall of 20 Codex and 15 Devin sessions (September 5–7, 2026) where orchestrate-tree was invoked at root, manager, and leaf levels across W08–W11 workstreams. Canonical owner: global `orchestrate-tree`; no ownership moves.

Observed failure modes with correction counts:

| Failure mode | Evidence | Correction |
| --- | --- | --- |
| Artificial numerical ceilings | Agents imposed one-child, 500 MB, no-delegation, 10 GiB/1 GiB thresholds, descendant/artifact/test ceilings across W08–W11; root user asked "why all these arbitrary numbers?" and "no arbitrary quotas or limits please" | New "Advance at full tempo" section: impose no numerical limit the user or actual resource constraints did not state; a measured baseline is not a hard quota |
| Failure to recursively delegate | "Apply orchestrate-tree literally: recursively delegate all independently ready outcomes now" repeated twice each across W08, W09, W10, W11; "each workstream needs to work faster and delegate more effectively"; "every single task from the plan needs to materially advance as early as the serial/parallel plan allows" | Same section: every independently ready outcome advances now; a node that holds ready work while waiting for a slot grant or review it was not asked to obtain wastes the denominator; scheduling step 2 now says do not wait for a slot grant the parent did not impose |
| Stopping on external errors | W09, W10 TRON, and W08 managers all "stopped on an external connection error" and halted their entire node instead of escalating one path and continuing independent work | Repair section: an external error on one path does not halt the node; escalate that path and continue independently ready work |
| Stale gate repetition | Migration-archives owner repeated prospective/handoff/intake gates after releases and activation already occurred; treated terminal controller as a live PID | Advance section: do not re-raise a gate that already completed; a settled boundary stays settled unless new evidence contradicts it |
| Readiness reports instead of concrete work | Data-candidates owner repeated readiness reports instead of advancing concrete source work; W09 was told "this must create source/candidate evidence, not just restate backlog" | Advance section: produce concrete deliverables, not restatements of readiness |
| Preparation vs execution confusion | Root had to clarify "accepts G20/G23 preparation, not transfer execution" and "accepted execution return as preparation only" | Advance section: accepting preparation evidence does not authorize execution |
| Skill read failures | Three Devin sessions (bitter-crop, understood-tomato, dark-vole) hit "Failed to read file" internal errors with no fallback | Ownership section: if the skill cannot be read at the child, the parent supplies the relevant decisions directly |
| Interchangeability ambiguity | The skill did not explicitly state that the same rules apply at every level; agents at manager and leaf level behaved as though some rules were root-only | Opening paragraph: the same ownership, delegation, and acceptance rules apply at every level; no rule applies only to the program root unless it says so |

Contrast cases: an actual resource constraint (measured disk, shared writer, OS memory pressure) legitimately gates the affected operation; a user-imposed limit is respected; a genuinely serial dependency chain does not require parallel dispatch; a parent that explicitly authorized execution has already made the execution decision. These corrections implement explicit user steering against observed tempo and scale failures across 35 sessions; no repeated comparative evaluation or measured speed improvement is claimed. Structural frontmatter, section, and whitespace checks passed; PyYAML was unavailable for the bundled validator.

### Worktree and branch discipline regression

Source: September 7 continued recall of the same 35 sessions, filtering for worktree, branch, main-write, and checkout-discipline corrections. Canonical owner: global `orchestrate-tree` for write containment and integration authority; `compose-stack-candidates` for worktree lifecycle and naming. No ownership moves.

Observed failure modes with correction counts (Codex user turns; Devin user turns in parentheses):

| Failure mode | Evidence | Correction |
| --- | --- | --- |
| Main write or main contamination | 60 "read-only", 15 "main is read-only", 13 "do not edit main", 13 "no main", 5 "no main write", 2 "never edit main", 2 "main stays read-only" across W08–W11, ATProto, PSBT, W05, W09 assignments; root had to repeatedly state "main is read-only" and "root owns main integration" | New "Confine writes to owned scope" section: the accepting parent owns promotion to shared or canonical state; a child that writes there without acceptance has crossed its integration boundary |
| Unrelated file mutation | PSBT assignment: "Existing route and OPML changes belong to other owners and must remain byte-untouched"; W05: "Preserve all runtime-owner dirty files and lock state"; W11: "current main was already dirty in SourceBinding.ts before this work — I preserved it"; 2 "preserve all unrelated", 2 "must remain byte" | Same section: unrelated files, sibling-owned paths, and existing checkout state belong to their owners; preserve them, do not edit or roll over them |
| Unauthorized commits in scratch/staging | W08: "its first child performed an unauthorized lock-only operation"; Tron leaf created an embedded Git commit in artifact staging despite output-only assignment; 2 "prohibited", 1 "unauthorized" | Same section: do not commit, stage, or embed Git commits unless the assignment explicitly authorizes it |
| System temp usage | Root correction: "please use dated subfolder in blockhead-2026-agent for scratch artifacts, not /tmp"; PSBT: "prohibited /private/tmp command; parent removed that exact owned directory"; 7 "system temp", 9 "/tmp", 1 "private/tmp" | Same section: use dated subdirectories under the owned output root, never system temporary directories |
| Worktree moves of dirty checkouts | Activation assignments: "Do not move active/dirty worktrees merely for naming"; 2 "do not move" | Same section: do not move, rename, rebase, or remove an active or dirty checkout to normalize its name; wait for a clean owner boundary |
| Sibling worktree/archive traversal | W11: "Do not rediscover the task through sibling archives"; ATProto: "do not search archives/other worktrees"; 12 "sibling" references | Same section: do not search sibling worktrees or archives for an assignment the parent already supplied |
| Missing HEAD/git-status evidence | X-route assignment: "Return exact diff, hashes, evidence, limitations, git status, and confirm HEAD unchanged"; assistant returns consistently showed "HEAD unchanged" and "git status: only the owned file modified" as required evidence | Return section: for checkout-based work, return working-tree status and confirm the base ref unchanged unless the assignment authorized a commit or ref change |

Contrast cases: a node explicitly authorized for integration may write to shared state; an authorized commit per `atomic-commits` is legitimate; a clean, process-free checkout may be migrated per `compose-stack-candidates`; a scoped lookup for a named missing dependency may broaden after naming what the supplied sources cannot resolve. The canonical text states these as general principles at any tree level; the evidence column records the specific history that motivated them. These corrections implement explicit user steering against observed write-containment failures across 35 sessions; no repeated comparative evaluation or measured reliability improvement is claimed. Structural and whitespace checks pass.
