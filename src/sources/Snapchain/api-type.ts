/**
 * Snapchain node HTTP API surface.
 *
 * These endpoints are served by Snapchain nodes and cover protocol-native,
 * on-network data access rather than proprietary indexing layers.
 *
 * @see https://snapchain.farcaster.xyz/reference/httpapi/httpapi
 * @see https://github.com/farcasterorg/hypersnap
 */
export enum ApiType {
	Rest = 'Rest',
}
