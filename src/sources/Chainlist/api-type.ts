/**
 * Chainlist — public chain registry (`rpcs.json` on chainlist.org), DefiLlama/chainlist.
 * @see https://chainlist.org/rpcs.json
 * @see https://github.com/DefiLlama/chainlist/blob/main/README.md#api
 */
export enum ApiType {
	Rest = 'Rest',
}
