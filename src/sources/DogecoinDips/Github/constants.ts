import { githubHttpEndpoints } from '$/sources/_shared/hosts/Github/Http/constants.ts'

export const dogecoinDipsGithubEndpoints = githubHttpEndpoints

export const dogecoinDipsGithubRepo = {
	owner: 'dogecoin',
	repo: 'dips',
	path: '',
	ref: 'master',
} as const
