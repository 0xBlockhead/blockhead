/**
 * EIP-1193 JSON-RPC via @tevm/voltaire (eth_getBlockByNumber, eth_getTransactionByHash, BlockStream).
 * @see https://www.npmjs.com/package/@tevm/voltaire
 */
export enum ApiType {
	JsonRpc = 'JsonRpc',
}
