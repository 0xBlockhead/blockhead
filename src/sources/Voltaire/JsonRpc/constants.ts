/**
 * Voltaire JsonRpc: methods come from `@tevm/voltaire/jsonrpc` `Rpc.Eth.*` in queries.ts.
 * Default RPC URLs: `$/constants/ExecutionEndpoints.ts` (endpoints, lookups, `default*ClientUrls`).
 */
