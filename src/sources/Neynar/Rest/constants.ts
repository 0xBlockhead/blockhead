/** Default `limit` for `GET /v2/farcaster/feed/` when omitted. */
export const neynarFeedDefaultLimit = 25

/** Maximum `limit` allowed by Neynar feed requests. */
export const neynarFeedMaxLimit = 100

/** Default `limit` for `GET /v2/farcaster/feed/user/casts/` when omitted. */
export const neynarUserCastsDefaultLimit = 25

/** Maximum `limit` allowed by Neynar user-cast requests. */
export const neynarUserCastsMaxLimit = 150

/** Maximum FIDs accepted by Neynar bulk-user and filtered-feed requests. */
export const neynarFidCountMax = 100
