<!-- Generated from APP.ts. Do not edit by hand. -->

<script lang="ts">
	// Types/constants
	import type { ComponentProps } from 'svelte'
	import type { EntityProxyData, EntityProxyResource } from '$/client/$proxy.svelte.ts'
	import type { WithRest } from '$/typescript/WithRest.ts'
	import EntityView, { EntityLayout } from '$/components/EntityView.svelte'
	import { EntityMetaKey } from '$/schema/$schema.ts'
	import { EntityType } from '$/schema/EntityType.ts'
	import { schema } from '$/schema/index.ts'


	// Context
	import { select } from '$/routes/+layout.svelte'


	// State
	let {
		selection,
		prefetched = {},
		title,
		href,
		layout = EntityLayout.SummaryDetails,
		open = $bindable(layout === EntityLayout.SummaryDetails),
		...EntityViewProps
	}: WithRest<
		{
			selection: EntityProxyResource<typeof schema, EntityType.StellarLiquidityPool_Timestamp>
			prefetched?: Partial<EntityProxyData<typeof schema, EntityType.StellarLiquidityPool_Timestamp>>
			title?: string
			href?: string
			layout?: EntityLayout
			open?: boolean
		},
		Pick<
			ComponentProps<typeof EntityView>,
			| 'collapsible'
			| 'showTypeAnnotation'
		>
	> = $props()

	const pendingEntity = $derived(({ ...prefetched[EntityMetaKey.Selector], ...selection.entitySelector, ...prefetched }))
	const stellarLiquidityPoolTimestamp = $derived(selection({}))
	const titleFallback = $derived('stellar liquidity pool timestamp')
	const viewDomId = $derived('stellar-liquidity-pool-timestamp-' + (titleFallback.toLowerCase().replace(/[^a-z0-9]+/g, '-') || 'entity').replace(/^-|-$/g, ''))


	// Components
	import ResourceBoundary from '$/components/ResourceBoundary.svelte'
	import TruncatedValue from '$/components/TruncatedValue.svelte'
	import StellarLiquidityPoolView from '$/views/StellarLiquidityPoolView.svelte'
</script>


<EntityView
	entityType={EntityType.StellarLiquidityPool_Timestamp}
	entitySelector={selection.entitySelector ?? prefetched[EntityMetaKey.Selector]}
	id={viewDomId}
	title={title ?? titleFallback}
	{href}
	{layout}
	bind:open
	{...EntityViewProps}
>
	{#snippet Title()}
		<ResourceBoundary resource={stellarLiquidityPoolTimestamp}>
			{#snippet Pending()}
				{title || 'stellar liquidity pool timestamp'}
			{/snippet}

			{#snippet children(entity)}
				{@const resolvedEntity = { ...pendingEntity, ...entity }}
				{title || titleFallback}
			{/snippet}
		</ResourceBoundary>
	{/snippet}

	{#snippet Content({ open: contentOpen })}
		<dl data-column-item="center">
			<div>
				<dt>liquidity pool</dt>
				<dd>
					<StellarLiquidityPoolView
						selection={select(EntityType.StellarLiquidityPool, selection.entitySelector.$liquidityPool, {})}
						layout={EntityLayout.Value}
						open={false}
					/>
				</dd>
			</div>

			<div>
				<dt>Timestamp</dt>
				<dd>
					<ResourceBoundary
						resource={
							selection({
								fields: {
									timestampMs: true,
								},
							})
						}
					>
						{#snippet Pending()}
							{@const timestampMs = pendingEntity.timestampMs}
							{#if timestampMs !== undefined && timestampMs !== null}
								<Timestamp timestamp={Number(timestampMs)} />
							{/if}
						{/snippet}

						{#snippet children(entity)}
							{@const resolvedEntity = { ...pendingEntity, ...entity }}
							{@const timestampMs = resolvedEntity.timestampMs}
							{#if timestampMs !== undefined && timestampMs !== null}
								<Timestamp timestamp={Number(timestampMs)} />
							{/if}
						{/snippet}
					</ResourceBoundary>
				</dd>
			</div>

			<div>
				<dt>Source</dt>
				<dd>
					<ResourceBoundary
						resource={
							selection({
								fields: {
									source: true,
								},
							})
						}
					>
						{#snippet Pending()}
							{@const source = pendingEntity.source}
							{#if source !== undefined && source !== null}
								{String((source) ?? '')}
							{/if}
						{/snippet}

						{#snippet children(entity)}
							{@const resolvedEntity = { ...pendingEntity, ...entity }}
							{@const source = resolvedEntity.source}
							{#if source !== undefined && source !== null}
								{String((source) ?? '')}
							{/if}
						{/snippet}
					</ResourceBoundary>
				</dd>
			</div>

			<ResourceBoundary
				resource={
					selection({
						fields: {
							ledgerSequence: true,
						},
					})
				}
			>
				{#snippet Pending()}
					{@const ledgerSequence = pendingEntity.ledgerSequence}
					{#if ledgerSequence !== undefined && ledgerSequence !== null}
						<div>
							<dt>ledger sequence</dt>
							<dd>
								{String((ledgerSequence) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}

				{#snippet children(entity)}
					{@const resolvedEntity = { ...pendingEntity, ...entity }}
					{@const ledgerSequence = resolvedEntity.ledgerSequence}
					{#if ledgerSequence !== undefined && ledgerSequence !== null}
						<div>
							<dt>ledger sequence</dt>
							<dd>
								{String((ledgerSequence) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}
			</ResourceBoundary>

			<ResourceBoundary
				resource={
					selection({
						fields: {
							reserveA: true,
						},
					})
				}
			>
				{#snippet Pending()}
					{@const reserveA = pendingEntity.reserveA}
					{#if reserveA !== undefined && reserveA !== null}
						<div>
							<dt>reserve a</dt>
							<dd>
								{String((reserveA) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}

				{#snippet children(entity)}
					{@const resolvedEntity = { ...pendingEntity, ...entity }}
					{@const reserveA = resolvedEntity.reserveA}
					{#if reserveA !== undefined && reserveA !== null}
						<div>
							<dt>reserve a</dt>
							<dd>
								{String((reserveA) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}
			</ResourceBoundary>

			<ResourceBoundary
				resource={
					selection({
						fields: {
							reserveB: true,
						},
					})
				}
			>
				{#snippet Pending()}
					{@const reserveB = pendingEntity.reserveB}
					{#if reserveB !== undefined && reserveB !== null}
						<div>
							<dt>reserve b</dt>
							<dd>
								{String((reserveB) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}

				{#snippet children(entity)}
					{@const resolvedEntity = { ...pendingEntity, ...entity }}
					{@const reserveB = resolvedEntity.reserveB}
					{#if reserveB !== undefined && reserveB !== null}
						<div>
							<dt>reserve b</dt>
							<dd>
								{String((reserveB) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}
			</ResourceBoundary>

			<ResourceBoundary
				resource={
					selection({
						fields: {
							totalShares: true,
						},
					})
				}
			>
				{#snippet Pending()}
					{@const totalShares = pendingEntity.totalShares}
					{#if totalShares !== undefined && totalShares !== null}
						<div>
							<dt>total shares</dt>
							<dd>
								{String((totalShares) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}

				{#snippet children(entity)}
					{@const resolvedEntity = { ...pendingEntity, ...entity }}
					{@const totalShares = resolvedEntity.totalShares}
					{#if totalShares !== undefined && totalShares !== null}
						<div>
							<dt>total shares</dt>
							<dd>
								{String((totalShares) ?? '')}
							</dd>
						</div>
					{/if}
				{/snippet}
			</ResourceBoundary>

			<ResourceBoundary
				resource={
					selection({
						fields: {
							accounts: true,
						},
					})
				}
			>
				{#snippet Pending()}
					{@const accounts = pendingEntity.accounts}
					{#if accounts !== undefined && accounts !== null}
						<div>
							<dt>accounts</dt>
							<dd>
								<TruncatedValue value={String((accounts) ?? '')} />
							</dd>
						</div>
					{/if}
				{/snippet}

				{#snippet children(entity)}
					{@const resolvedEntity = { ...pendingEntity, ...entity }}
					{@const accounts = resolvedEntity.accounts}
					{#if accounts !== undefined && accounts !== null}
						<div>
							<dt>accounts</dt>
							<dd>
								<TruncatedValue value={String((accounts) ?? '')} />
							</dd>
						</div>
					{/if}
				{/snippet}
			</ResourceBoundary>
		</dl>
	{/snippet}
</EntityView>
