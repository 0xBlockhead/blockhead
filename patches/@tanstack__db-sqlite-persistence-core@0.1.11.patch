diff --git a/dist/cjs/persisted.cjs b/dist/cjs/persisted.cjs
index f53706d9d6e14a04d33497fd639eefff8a924973..f5047733e5eb2f3491bfa8648851d32042382c8d 100644
--- a/dist/cjs/persisted.cjs
+++ b/dist/cjs/persisted.cjs
@@ -360,18 +360,9 @@ class PersistedCollectionRuntime {
     );
     if (upstreamLoadSubset) {
       try {
-        const maybePromise = upstreamLoadSubset(options);
-        if (maybePromise instanceof Promise) {
-          maybePromise.catch((error) => {
-            console.warn(
-              `Failed to load remote subset in persisted wrapper:`,
-              error
-            );
-            this.queueRemoteSubsetEnsure(options);
-          });
-        }
+        await upstreamLoadSubset(options);
       } catch (error) {
-        console.warn(`Failed to trigger remote subset load:`, error);
+        console.warn(`Failed to load remote subset in persisted wrapper:`, error);
         this.queueRemoteSubsetEnsure(options);
       }
     }
@@ -386,6 +377,10 @@ class PersistedCollectionRuntime {
       () => this.hydrateSubsetUnsafe(options, { requestRemoteEnsure: false })
     );
   }
+  async waitForPersistence() {
+    await this.applyMutex.run(async () => {
+    });
+  }
   queueHydrationBufferedTransaction(transaction) {
     this.queuedHydrationTransactions.push(transaction);
   }
@@ -1632,7 +1627,8 @@ function persistedCollectionOptions(options) {
   const persistedUtils = {
     acceptMutations,
     getLeadershipState: () => runtime.getLeadershipState(),
-    forceReloadSubset: (subsetOptions) => runtime.forceReloadSubset(subsetOptions)
+    forceReloadSubset: (subsetOptions) => runtime.forceReloadSubset(subsetOptions),
+    waitForPersistence: () => runtime.waitForPersistence()
   };
   const mergedUtils = {
     ...localOnlyOptions.utils ?? {},
diff --git a/dist/esm/persisted.js b/dist/esm/persisted.js
index 612e7f6f097b0abe5c2bccb15eebd4fcabd4605a..f8cbad2e98c284bca05094917ddeb82a87452401 100644
--- a/dist/esm/persisted.js
+++ b/dist/esm/persisted.js
@@ -358,18 +358,9 @@ class PersistedCollectionRuntime {
     );
     if (upstreamLoadSubset) {
       try {
-        const maybePromise = upstreamLoadSubset(options);
-        if (maybePromise instanceof Promise) {
-          maybePromise.catch((error) => {
-            console.warn(
-              `Failed to load remote subset in persisted wrapper:`,
-              error
-            );
-            this.queueRemoteSubsetEnsure(options);
-          });
-        }
+        await upstreamLoadSubset(options);
       } catch (error) {
-        console.warn(`Failed to trigger remote subset load:`, error);
+        console.warn(`Failed to load remote subset in persisted wrapper:`, error);
         this.queueRemoteSubsetEnsure(options);
       }
     }
@@ -384,6 +375,10 @@ class PersistedCollectionRuntime {
       () => this.hydrateSubsetUnsafe(options, { requestRemoteEnsure: false })
     );
   }
+  async waitForPersistence() {
+    await this.applyMutex.run(async () => {
+    });
+  }
   queueHydrationBufferedTransaction(transaction) {
     this.queuedHydrationTransactions.push(transaction);
   }
@@ -1630,7 +1625,8 @@ function persistedCollectionOptions(options) {
   const persistedUtils = {
     acceptMutations,
     getLeadershipState: () => runtime.getLeadershipState(),
-    forceReloadSubset: (subsetOptions) => runtime.forceReloadSubset(subsetOptions)
+    forceReloadSubset: (subsetOptions) => runtime.forceReloadSubset(subsetOptions),
+    waitForPersistence: () => runtime.waitForPersistence()
   };
   const mergedUtils = {
     ...localOnlyOptions.utils ?? {},
diff --git a/src/persisted.ts b/src/persisted.ts
index 7c20e96883f9a9c0365cd50cca94311c59e40912..57b313e1e4cb761319f14796f978aecb1f3da396 100644
--- a/src/persisted.ts
+++ b/src/persisted.ts
@@ -345,6 +345,7 @@ export interface PersistedCollectionUtils extends UtilsRecord {
   }) => Promise<void> | void
   getLeadershipState?: () => PersistedCollectionLeadershipState
   forceReloadSubset?: (options: LoadSubsetOptions) => Promise<void> | void
+  waitForPersistence: () => Promise<void>
 }
 
 export type PersistedSyncWrappedOptions<
@@ -988,18 +989,9 @@ class PersistedCollectionRuntime<
 
     if (upstreamLoadSubset) {
       try {
-        const maybePromise = upstreamLoadSubset(options)
-        if (maybePromise instanceof Promise) {
-          maybePromise.catch((error) => {
-            console.warn(
-              `Failed to load remote subset in persisted wrapper:`,
-              error,
-            )
-            this.queueRemoteSubsetEnsure(options)
-          })
-        }
+        await upstreamLoadSubset(options)
       } catch (error) {
-        console.warn(`Failed to trigger remote subset load:`, error)
+        console.warn(`Failed to load remote subset in persisted wrapper:`, error)
         this.queueRemoteSubsetEnsure(options)
       }
     }
@@ -1020,6 +1012,10 @@ class PersistedCollectionRuntime<
     )
   }
 
+  async waitForPersistence(): Promise<void> {
+    await this.applyMutex.run(async () => {})
+  }
+
   queueHydrationBufferedTransaction(
     transaction: BufferedSyncTransaction<T, TKey>,
   ): void {
@@ -2711,6 +2707,7 @@ export function persistedCollectionOptions<
     getLeadershipState: () => runtime.getLeadershipState(),
     forceReloadSubset: (subsetOptions: LoadSubsetOptions) =>
       runtime.forceReloadSubset(subsetOptions),
+    waitForPersistence: () => runtime.waitForPersistence(),
   }
 
   const mergedUtils = {
